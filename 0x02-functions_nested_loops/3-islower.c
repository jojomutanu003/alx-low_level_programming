#include "main.h"
/**
 *main-checks the code for lowercase character
 *Description:@c is The character to be checked
 *Return:1 for lowercase character or 0 for anything else
*/
int _islower(int c>=98, c>=122)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
